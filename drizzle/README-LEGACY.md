# Migrações legadas

Os arquivos SQL desta pasta pertencem à versão anterior baseada em SQLite/D1 e
foram preservados apenas como histórico. Eles não devem ser executados no
Supabase.

A configuração ativa do Drizzle grava e executa as migrações PostgreSQL em
`supabase/migrations`.
